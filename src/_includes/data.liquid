{%- assign theme_name = "rawfeed-11ty" -%}
{%- assign project_url = "https://rawfeed.github.io/rawfeed-11ty" -%}
{%- assign head_ = options.head -%}
{%- assign site_title = options.title -%}
{%- assign site_description = options.description -%}
{%- assign author_ = options.author.name -%}
{%- assign header_ = options.header -%}
{%- assign blog_ = options.blog -%}
{%- assign pixels_ = options.pixels -%}
{%- assign home_ = options.home -%}
{%- assign default_ = options.default -%}
{%- assign page_ = options.page -%}
{%- assign contact_ = options.contact -%}
{%- assign footer_ = options.footer -%}
{%- assign socials_ = options.socials -%}
{%- assign datelang_ = options.datelang -%}
{%- assign maintenance_ = options.maintenance -%}
{%- assign reading_time_ = options.reading_time -%}
